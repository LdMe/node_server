extends ../layout 

block content
    h1=player.name 
    h2=player.last_name 
    h3=player.age 
    if player.team 
        h4=player.team.name 
    else
        h4 Sin Equipo 
    a(href="/players" class="btn") Volver 
